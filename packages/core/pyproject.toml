[project]
name = "sway-core"
version = "0.1.0"
description = "Shared domain logic for Sway desktop, API, and web."
requires-python = ">=3.12"
dependencies = [
    "python-dateutil>=2.9.0.post0",
]

[tool.setuptools.packages.find]
include = ["sway_core*"]
